# QuantResearch parquet cache (rebuilt from Data/ on demand)
Data/.qr_cache/
.qr_cache/

# Saved backtest runs: ignore timestamped runs + master log, but keep the
# always-current latest/ snapshot under version control for future reference.
results/*
!results/latest/

# Python
__pycache__/
*.pyc

# Node (docx report generator deps)
node_modules/
package.json
package-lock.json

# macOS
.DS_Store
